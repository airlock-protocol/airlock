{
  "private": true,
  "name": "airlock-workspace",
  "workspaces": [
    "sdks/typescript",
    "integrations/airlock-mcp"
  ],
  "scripts": {
    "build:sdk": "npm run build -w airlock-client",
    "build:mcp": "npm run build -w airlock-mcp",
    "build:js": "npm run build:sdk && npm run build:mcp"
  },
  "engines": {
    "node": ">=18"
  }
}
